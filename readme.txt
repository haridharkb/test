The course is done in udemy 
git is an important topic which we need to know and learn!!
git pull is used to check whether the new status or an update in an master 
and if once the branch is  created to switch to it from master we have command as git checkout "branch_name"